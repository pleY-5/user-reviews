config:
  target: "http://localhost:3002"
  phases:
    - duration: 60
      arrivalRate: 100
  payload:
    path: "reviewId.csv"
    fields: 
      - "restaurant_id"
scenarios:
  - flow:
    - get:
        url: "/api/reviews/{{ restaurant_id }}"



